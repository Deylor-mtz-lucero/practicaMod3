services:
  postgres:
    #image: postgres:15
    build: . #Construir contenedor usando Dockerfile presente en el directorio actual (.)
    container_name: ContDBpractMod3C # Nombre del contenedor
    ports:
     - "5432:5432" # Puerto para conectar desde tu máquina host al contenedordocker stop ContDBpractMod33
    environment:
      POSTGRES_USER: Admin # Usuario de la base de datos
      POSTGRES_PASSWORD: p4ssw0rdDB # Contraseña del usuario
      POSTGRES_DB: credencialUsuario # Nombre de la base de datos no me lo quiere tomar
    volumes:
      # Espacio donde los datos se conservarán, al detener el contenedor.
      - postgres_data:/var/lib/postgresql/data 
volumes:
  postgres_data:
    driver: local
